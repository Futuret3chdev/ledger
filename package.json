{
  "name": "ledger",
  "version": "1.0.0",
  "private": true,
  "description": "Futuret3ch Ledger — schedule bills and upcoming payments in AUD.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host --port 4179",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.3.3"
  },
  "devDependencies": {
    "vite": "^6.3.6"
  }
}
