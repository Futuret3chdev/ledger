{
  "name": "ledger",
  "version": "1.0.0",
  "private": true,
  "description": "JAX by Futuret3ch and MemeTorrent — books for the MT ECO SYSTEM.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host --port 4179",
    "test": "node --test tests/*.test.js",
    "feed": "node sdk/jax-feed/bin/jax-feed.mjs"
  },
  "dependencies": {
    "@vercel/blob": "^2.3.3"
  },
  "devDependencies": {
    "vite": "^6.3.6"
  }
}
